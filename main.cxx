#include "iostream"
//#include "stdexcept"
//#include "step.h"

int main(int argc, char **argv) {
  //if (argc != 2) {
  //  throw std::invalid_argument( "Invalid number of arguments. Syntax is: tornado step_file.stp" );
  //}
  std::cout << "Hello";
  //Step a = Step(argv[1]);
  return 0;
}
